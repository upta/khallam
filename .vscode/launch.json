{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open the site",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/site",
      "preLaunchTask": "site: dev server"
    },
    {
      "name": "Open the site (Edge)",
      "type": "msedge",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/site",
      "preLaunchTask": "site: dev server"
    }
  ]
}
